spring.datasource.url=jdbc:postgresql://${MEDICAL_DB_HOST:localhost}:${MEDICAL_DB_PORT:5445}/${MEDICAL_DB_NAME:afya_medical}
spring.datasource.username=${MEDICAL_DB_USER:afya}
spring.datasource.password=${MEDICAL_DB_PASSWORD:afya}
spring.datasource.driver-class-name=org.postgresql.Driver

app.jwt.access-secret=${JWT_ACCESS_SECRET:dev-access-secret-at-least-64-characters-long-for-hs512-signing-key}
