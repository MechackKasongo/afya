spring.datasource.url=jdbc:postgresql://${STAY_DB_HOST:stay-db}:5432/${STAY_DB_NAME:afya_stay}
spring.datasource.username=${STAY_DB_USER:afya}
spring.datasource.password=${STAY_DB_PASSWORD:afya}
spring.datasource.driver-class-name=org.postgresql.Driver

app.jwt.access-secret=${JWT_ACCESS_SECRET:dev-access-secret-at-least-64-characters-long-for-hs512-signing-key}

app.services.audit-base-url=${AUDIT_SERVICE_BASE_URL:http://audit:8087}
