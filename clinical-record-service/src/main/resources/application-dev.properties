spring.datasource.url=jdbc:postgresql://${CLINICAL_DB_HOST:localhost}:${CLINICAL_DB_PORT:5438}/${CLINICAL_DB_NAME:afya_clinical}
spring.datasource.username=${CLINICAL_DB_USER:afya}
spring.datasource.password=${CLINICAL_DB_PASSWORD:afya}
spring.datasource.driver-class-name=org.postgresql.Driver

app.jwt.access-secret=${JWT_ACCESS_SECRET:dev-access-secret-at-least-64-characters-long-for-hs512-signing-key}
