spring.datasource.url=jdbc:postgresql://${HOSPITAL_DB_HOST:localhost}:${HOSPITAL_DB_PORT:5447}/${HOSPITAL_DB_NAME:afya_hospital}
spring.datasource.username=${HOSPITAL_DB_USER:afya}
spring.datasource.password=${HOSPITAL_DB_PASSWORD:afya}
spring.datasource.driver-class-name=org.postgresql.Driver

app.jwt.access-secret=${JWT_ACCESS_SECRET:dev-access-secret-at-least-64-characters-long-for-hs512-signing-key}
