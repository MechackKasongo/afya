spring.datasource.url=jdbc:postgresql://${AUDIT_DB_HOST:audit-db}:5432/${AUDIT_DB_NAME:afya_audit}
spring.datasource.username=${AUDIT_DB_USER:afya}
spring.datasource.password=${AUDIT_DB_PASSWORD:afya}
spring.datasource.driver-class-name=org.postgresql.Driver

app.jwt.access-secret=${JWT_ACCESS_SECRET:dev-access-secret-at-least-64-characters-long-for-hs512-signing-key}
